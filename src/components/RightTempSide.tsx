import React from "react";

const RightTempSide = () => {
  return (
    <div className="flex flex-1 flex-col md:flex-wrap">
      <canvas className="flex flex-1 flex-col bg-orange-400 md:flex-wrap"></canvas>
    </div>
  );
};

export default RightTempSide;
